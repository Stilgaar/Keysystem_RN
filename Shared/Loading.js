import { View, Text } from "react-native";

// will be replaced with a spinner later

function Loading() {
    return (

        <View>
            <Text>Loading ...</Text>
        </View>
    );
}

export default Loading;